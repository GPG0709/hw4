<!DOCTYPE html>
<html>
  <head>
    <title>Travel journal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="/stylesheets/application.css">
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
      <a class="navbar-brand" href="/">Travel Journal</a>
      <ul class="navbar-nav">
        <% if @current_user %>
          Hi, <%= @current_user["username"] %> |
          <a class="nav-link active" aria-current="page" href="/logout" class="btn btn-primary">Logout</a>
        <% else %>
          <a class="nav-link active" aria-current="page" href="/login" class="btn btn-default">Login  |</a>
          <a class="nav-link active" aria-current="page" href="/users/new" class="btn btn-default">Sign Up</a>
        <% end %>
      </ul>
      </div>
    </nav>

  <div class="container mt-2">
    <div class="row">
      <%= flash["notice"] %>
    </div>
    <%= yield %>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2" crossorigin="anonymous"></script>
  </body>
</html>
